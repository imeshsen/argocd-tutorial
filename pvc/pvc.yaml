apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-tekton-pvc
  namespace: oc-demo
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 200mi
